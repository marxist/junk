"1.0b7.2 "(created 2011/08/05 11:03:59)"

loadplugins '\.(js|penta)$'
group user

""" Prev and next tab with <count> support """
map -modes=n,v š -builtin <C-PageUp>
map -modes=n,v đ -builtin <C-PageDown>

""" Prev and next tab with <count> support """
map -modes=n,v ć -builtin <decrement-url-path>
map -modes=n,v ž -builtin <increment-url-path>

""" remap Ctrl-stuff to original actions """
map -modes=n,v,i,c <C-a> -builtin <pass-next-key><C-a>
map -modes=n,v,i,c <C-c> -builtin <pass-next-key><C-c>
map -modes=n,v,i,c <C-x> -builtin <pass-next-key><C-x>
map -modes=n,v,i,c <C-y> -builtin <pass-next-key><C-y>
map -modes=n,v,i,c <C-z> -builtin <pass-next-key><C-z>
map -modes=n,v,i,c <C-v> -builtin <pass-next-key><C-v>

" set defsearch=duckduckgo-ssl
set editor=geany

" vim: set ft=pentadactyl:
